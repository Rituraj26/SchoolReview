import React, { Fragment } from 'react';

const NoTeacherComponent = () => {
    return (
        <Fragment>
            <p className="lead">You have not yet added any courses</p>
        </Fragment>
    );
};

export default NoTeacherComponent;
